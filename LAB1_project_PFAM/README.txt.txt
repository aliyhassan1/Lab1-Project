In this repository you can find a notebook containing the script using to build a kunitz_domain_predicting model as a jupyter file 
You can also find the following files:
  1)benchmark1 and benchmark2 for training the model
  2)kunitz.hmm the model itself
  3)kucitz.ali the 3D alignment of the proteins used to build the model